'use strict';
const cats = require('../../../static_categories');
// const strapi = require('strapi')

/**
 * Read the documentation (https://strapi.io/documentation/v3.x/concepts/controllers.html#core-controllers)
 * to customize this controller
 */

module.exports = {
  // async find(ctx){
  //   /*const categories = await strapi.services.category.find();
    
  //   const proms = [];
  //   categories.forEach(({ id, super_collection }) => {
  //     const super_collection = url.split('/')[2].split('?')[0]
  //     proms.push(strapi.services.category.update({ id }, { super_collection }))
  //   })
  //   await Promise.all(proms); */

  //   let products = await strapi.services.products.find();
  //   await Promise.all(products);
  //   ctx.send('done')
    
  // }
};
